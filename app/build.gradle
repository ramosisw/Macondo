apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'ramos.isw'
            keyPassword 'cule@160'
            storeFile file('C:/Users/Admin/Google Drive/AndroidDev/StoreKey/keystore.culeAt160.jks')
            storePassword 'cule@160'
        }
    }
    compileSdkVersion 22
    buildToolsVersion "22.0.1"

    defaultConfig {
        applicationId "mx.itson.macondo"
        minSdkVersion 14
        targetSdkVersion 22
        versionCode 2
        versionName "1.1"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile 'com.android.support:appcompat-v7:22.+'
    compile 'com.google.android.gms:play-services:4.3.23'
    compile 'com.shamanland:fab:0.0.8'
}

